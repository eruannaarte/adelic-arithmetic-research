# Independent audit of Path 1

**Verdict: pass for the stated finite source/preparation boxes, shared clock, sensor contract, and weighted adversarial noise model.** No unresolved mathematical defect was found in the canonical result. This review checks the proof and its implementation and reruns the nonlinear certificate; it is not proof-assistant verification or an independent implementation of Arb.

## Proof and model connection

The fixed-left-inverse theorem in `PROOF.md` is valid. Keeping one preparation fixed while integrating the source segment is legitimate because the source box is convex and the Jacobian enclosure is uniform over preparations. The remaining difference between two preparations is bounded by twice the uniform bias. Applying the same fixed matrix B to every term gives the stated row inequalities, and `k<1` permits rearrangement. The clock column is included in the full three-column perturbation; the proof does not incorrectly reuse nominal nuisance annihilation at unknown sources. Weighted Cauchy–Schwarz yields the stated noise gains. The sharper component bound follows by substituting the joint diameter back into the row inequality. The implemented `physical_pair_error_bound` uses separate maxima over the first two rows, which is a slightly weaker but valid upper bound.

I inspected `preparation.py:19`, the mechanical mass-matrix equations in `double_pendulum_dynamics.py`, and the nested `ParameterDual`/`IntervalJet` implementation in `oig_double_pendulum_validated_transfer.py`. The state equations match the declared mechanics with m1=l1=g=1. The determinant is positive for every finite state and log-parameter, so the field is smooth. The forward parameter sensitivities correctly hold preparation fixed; their initial values are zero. The outer derivative arithmetic includes the finite parameter and clock coordinates. The endpoint identity `tau * rhs(current)[sensor]` is the exact log-clock derivative for the autonomous time-dilated flow, including finite preparation. One globally shared clock is consistent with jointly enclosing each launch at the same clock value; separate launch enclosures conservatively discard correlation without assuming separate clocks.

The polynomial coefficients in `preparation.py:34` are actual dyadic numbers. At `preparation.py:47`, interval substitution at every center in the whole time segment bounds the order-q coefficient of the defect, equivalently its qth derivative divided by q!. Taylor's theorem then justifies retaining lower coefficients at zero and a uniform qth remainder. The strict tube gate at `preparation.py:69` supplies the first-exit contradiction and a valid endpoint enclosure. The upper-left four-by-four derivative block used for preparation is exactly the state derivative at shared parameters. Both compared trajectories, and their connecting state segment, lie in the accepted rectangular state tube. Consequently the separate preparation inequality contains no parameter forcing and needs no polynomial defect. Its homogeneous exact inequality also justifies scaling to every smaller preparation radius; outward numerical slack can be scaled with the witness bounds and remains conservative. Nonfinite derivative/defect evaluations fail closed through the shared magnitude helper.

The source-to-matrix connection therefore comes from the analytic enclosure proof plus the rebuilt interval integration, not from the nominal matrix or its hash alone. The fast rational checker is appropriately documented as conditional on the supplied interval enclosures. Its separate deep replay reconstructs every scientific/input/provenance field except descriptive elapsed time.

The local existence and smooth-parameter dependence assumptions were checked against [Teschl's author-hosted book](https://www.mat.univie.ac.at/~gerald/ftp/book-ode/ode.pdf), especially Theorem 2.11 on printed pages 47–48. Its smooth-field hypothesis and constant-parameter construction apply here. The accepted compact tubes provide continuation for the finite observation intervals.

## Fresh verification

Using Python 3.12.14 and python-flint 0.9.0 in `/private/tmp/math-five-paths-venv`, I freshly ran `validate.py`: **all 9 tests passed** in 8.4 seconds. This included complete nominal and finite-box source replay, the alternative 192-bit/order-9 partition, independent mechanical RHS and finite-difference checks, finite preparation/clock controls, and corrupted-enclosure rejection. The standard-library checker also passed separately.

Without importing `checker.py`, I independently reconstructed all three fixed-left-inverse identities, interval deviation row sums, preparation biases, rational upper square-root inequalities, and final diameters from the JSON files using `fractions.Fraction`. All exact values agreed. The diameter values were approximately 2.0116270417712133e-5, 2.4891410233236215e-5, and 1.9792376242283727e-5 for selected A+B, uniform A+B+C, and uniform A+B respectively.

As an additional control outside the author's suite, I replaced the RHS only in an isolated Python process with a diagonal linear flow having state rates (1,-2,3,-4), eleven constant augmented coordinates, state centers (1/4,-1/3,2/5,-3/7), and initial preparation radius 1e-7. After 100 outward steps of length 1/100, all four endpoint boxes contained the analytic interval solutions `initial_i * exp(rate_i)`, and all four propagated preparation bounds exceeded `1e-7 * exp(rate_i)`. A length-one step failed the sufficient tube gate, as expected for this absolute-Jacobian construction, despite the analytic flow existing globally. This checks the implementation's enclosure and failure semantics on an exactly solvable different field; the process did not modify any source file.

## Scope and review note

The reported design ranking compares sufficient certified bounds in a declared noise metric. It does not prove true minimax superiority, attainable hardware precision, or a globally successful numerical estimator. Uniform A+B correctly remains better than selected A+B in the primary preparation-dominated comparison. Exact recovery with positive unknown preparation/noise is not claimed. The source box, unwrapped sensor convention, positive active weights, and common clock are essential hypotheses.

One nonblocking input-hardening suggestion was reported and corrected: the fast checker's `verify_inputs` initially did not inspect the descriptive `preparation_coordinates` field. Deep source replay already inspected it through full reconstruction, so altered preparation semantics could not pass the complete certificate. The owner added a literal check and a mutation regression; I independently reran that focused test and it passed. No producer or scientific artifact changed, and no review issue remains open.

Reviewed files are under `/Volumes/KINGSTON/Vibecoding/Math/research/five_paths_2026_09/path1_preparation/`, together with the two named root mechanical/interval modules. Residual trusted components are Arb's inclusion arithmetic and series operations, Python's integer arithmetic, and the supplied analytic enclosure proof. The numerical held-out controls support the implementation review but do not replace these proof components.
