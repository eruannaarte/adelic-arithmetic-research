**Noise-aware arithmetic sensing comparison**

Freeze before new computations: compare the existing positive nested AS-V measure with its T=1780,m=8900 single-window control on exactly the same centered 8,900 physical times. Same degree-14 nonnegative integer coefficient envelope, N=50,sigma=2; same actual complex sensor-noise vector. The inner-grid contribution is added to the weight of the same reading, so covariance contains cross terms.

Primary quantities: complete deterministic coefficient-bias bounds; exact formula D G^{-1} Phi* W Sigma W Phi G^{-1} D for coefficient-noise covariance; rigorous per-coordinate noise-gain upper bounds; an explicit sufficient all-target bounded-noise radius; and a rigorous circular-Gaussian union failure bound. Norms and probability assumptions must be stated. A no-noise single-window bound above 1/2 is a failure of that sufficient certificate, not a recovery impossibility claim.

Secondary comparison: common-tail midpoint centering, when its evaluation error is explicitly included, may be applied symmetrically to both designs. Also compare the actual variance inflation induced by multiscale weighting; a bias advantage need not imply lower variance. Discovery floating calculations are labeled until enclosed or replaced by exact inequalities.

Validation: rebuild the complete multiscale dependency, and enough of the single-window dependency to support the comparison. Independently reconstruct weights and the Gram/noise consequences. Boundary tests include reused-reading covariance, incorrect independent-component covariance, rounding threshold, and distribution/norm assumptions.
