# Independent review of the unified synthesis

Reviewer: `/root/polynomial_drift`, 2026-09-05. **Verdict: the root report and reproduction guide accurately synthesize the six proof tracks under their declared scopes.** The work supports a unified research line through explicit transfer arguments and a common certificate adapter. It does not claim that the different models share one source class, noise norm, resource count, or sharp optimality theorem.

This was a bounded synthesis review against the individual proofs, reports, audits, evidence and verification wrapper. I did not broaden the research or rerun the consolidated suite. The common 687-gate adapter and the framework, preparation, noise and resolution mathematics received my separate reviews recorded in their audit files; harmonic and quadratic claims were compared against their independently audited witnesses and reconstructions.

## Scope and quantitative conclusions

- **Common framework:** the report correctly separates exact answer sets from conservative outer sets, and sound decisions from efficient numerical implementation. I clarified that the smallest enclosing radius is the best conditional worst-case error at a particular observation; uniform minimax error is its supremum over promised observations. The unrestricted-nuisance lifting result retains the actual positive weighted metric and leaves other structured uncertainties subject to their joint constraints. The three-model adapter's 243 quadratic, 441 polynomial and three nonlinear preparation gates total 687; they are shared algebraic consequences, not 687 independent research results.

- **Harmonic acquisition:** the six-dimensional time box has independently bounded perturbations of ±0.000025. The quantifier is one colliding pair for each schedule, with the pair allowed to vary. The fixed prime-3 probability coordinates give exactly the stated 0.549601355082555602 scalar gap and its half-gap minimax obstruction. Vertex floor, positive probabilities and full-source separation have the correct norms and remain uniform over the box. The report does not extrapolate to all schedules below time 30 or confuse a failed sufficient gate with this actual collision.

- **Preparation calibration:** the quoted diameter, row slack and noise-boundary values match the exact certificate. The source is the two log ratios and one shared clock, with infinity-norm diameter; deleting the clock coordinate preserves the stated physical log-query diameter. The known centers, local widths and shared systematic corrections remain distinct. The report makes calibration acquisitions and reference-characterization resources additional to the five dynamical outputs. The worked record is synthetic, physical status remains NOT_RUN, and the protocol cannot authenticate empirical bounds from evidence-reference strings or simulation results.

- **Quadratic arithmetic:** the finite nonlinear template model retains uncertain a(7), rather than selecting it by an unproved preliminary estimate. The independent witness table establishes actual positive fundamental discriminants no larger than 1,365 for all 81 combinations. The small conditional gains, the 0.02332810 largest declared radius, and the unchanged worst-case boundary refer to the specified sufficient certificate. The report correctly distinguishes the genuine boundary comparison from rounded reporting values and does not claim a sharp actual-sensor minimax threshold. I replaced “sensor covariance” with “weighted sensor-noise norm” in the slab explanation to avoid implying a statistical noise assumption where the theorem uses a deterministic error ball.

- **Polynomial drift:** the nine declared degree/design radii agree with the complete certificates. The same 8,900 readings support multiscale quintic drift and outer quartic drift, but each weight design defines its own physical noise ball. The known first coefficient, arbitrary complex drift in the exact model, complete infinite nuisance tails, and digital correction error are retained. The degree-five outer and degree-six failures remain failures of this sufficient bound; the degree-8,899 interpolation and unknown-constant aliases are separate exact obstructions. A deployed numerical solve and nonpolynomial residuals require the expressly stated error budgets.

- **Spatial sensing:** 50 rows preserve more than 99%, and 48 preserve more than 97%, of all original central calibrated floors, with the same source-relative allowances. The model keeps known time, two real source coordinates, and 21 integer target positions. I made the integer placement restriction explicit. The centroid proof is distinct from the source information-floor proof, and the lower bound of four real rows is necessary rather than sufficient. Direct spatial-average availability is required for acquisition savings. I replaced “saves processing only” with the more precise statement that forming the bank from already acquired modal data is post-processing and does not reduce acquired measurements; no processing-cost improvement has been proved.

## Information, resources and validation

The report expressly recognizes that known deterministic centering is invertible. Its prior improvement greater than 127-fold concerns a particular decoder's certified noise allowance, not increased intrinsic information in a fixed observation. The new quintic improvement likewise comes from a sharper complete nuisance-channel certificate and augmented fit. The exact quotient, conservative finite approximation and restricted acquisition have separate mathematical roles throughout the synthesis.

The wrapper contains 13 application/reconstruction jobs, six isolated suites with 6+10+10+7+8+8=49 focused tests, and two optional deep jobs, totaling 21 with `--deep`. The deep jobs are the three preparation ODE reconstructions and the 192-case finite-graph diagnostic. The guide correctly distinguishes new model-to-enclosure reconstructions from unchanged inherited d₂/d₁₄ tail and central-resolution premises. The large-state floating cases are diagnostics, not sampled substitutes for uniform proofs. The consolidated validation result itself is recorded by the root run; this synthesis review does not assert a separate rerun.

All wording changes were confined to the root report and guide. No scientific code, numerical certificate or prior research artifact was changed. The conclusions remain model-based mathematical guarantees with explicit numerical trust components and remaining empirical inputs. No historical-priority, publication, or physical-apparatus validation claim was introduced.
