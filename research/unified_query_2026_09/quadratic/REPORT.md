# A joint arithmetic query with an uncertain intermediate label

The new query uses all16 retained coefficients supported on primes2,3,5,7 that contain5 or7. After certifying a(2),a(3), it considers all nine possibilities for a(5),a(7), including the nonlinear a(25),a(49),a(50) relations. Every candidate pattern is realized by an actual real quadratic field. The independent audit supplies concrete fundamental discriminants at most1,365 covering all81 anchor/template combinations.

The central advance is that a(7) need not be separately identified. The decoder keeps every arithmetic template consistent with a family of rigorously bounded directional observations and answers only when all surviving templates agree on a(5). A true template always survives. Below its proved conditional margin, every wrong query is rejected. The decoder itself never assumes the true template or chooses its budget from an unknown label.

All243 relevant pair bounds retain the complete infinite tail and the actual covariance of the same8,900 readings. They do not give independent noise budgets to decoded coordinates. The resulting81 conditional sufficient radii never decrease relative to Q3 under the same anchor and weighted sensor norm. The maximum relative gain is just over0.4%; the largest absolute declared radius is0.02332810, compared with the previous largest0.02324. The meaningful boundary comparison in that all-split case is an improvement over0.3560%, not an inference from rounded reporting values.

This is a small additional gain, and the worst-case boundary remains exactly unchanged. When7 is inert and the two query labels are0 and1, the additional coordinates carry no separating information. That equality prevents a universal improvement for this certificate. Conversely, split5 supplies extra information through a(25), and split7 supplies it through a(35). The method can use that information even when the old direct-rounding certificate for a(7) cannot pass at the chosen noise level.

The result instantiates the common answer-set transfer theorem: finite template slabs are conservative enclosures of full-field observations; a singleton query set gives a valid answer. It also accepts explicit per-coefficient numerical-error bounds, which are added to the tail support before any decision. The published radius table concerns the exact mathematical sensing inverse. Actual numerical inversion needs its own bounded error contract.

The package has a full proof, a strict callable certificate checker and abstaining decoder, seven focused tests, an independent field-arithmetic implementation, and source-bound complete-tail premises. The full-grid examples are numerical interface controls, not measurements. The next question is whether exact directional Gram geometry or joint bias/noise optimization can improve these small conditional margins without adding observations.
