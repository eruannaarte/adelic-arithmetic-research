# Objective, result, and successor

**Starting point.** The preceding preparation study gave an almost volume-optimal anisotropic box for a fixed five-reading experiment. Its successor was to determine the joint tolerances an apparatus could achieve. The user also pointed to internally tested pendulum laboratory work.

**Objective.** Inspect the available laboratory's calibration semantics, and create a concrete mathematical bridge from measured initial-state intervals to the existing finite nonlinear certificate. Require honest treatment of shared systematic correction, separate bounded launch residuals, velocity/clock units, and physical provenance. Use an interior acceptance specification with substantial slack rather than the almost binding volume maximizer.

**Accomplished.** A conditional support theorem removes known measured center offsets from the ambiguity radius while retaining them in the domain test. A second shared-column projection accounts for the calibration instrument's cross-launch systematic offset. A strict record schema and exact checker certify a concrete synthetic specification with source diameter below 0.000060053, more than 35.79% target slack, and an exact preparation/noise tradeoff. The original zero-centered enclosure of the same support fails the declared target test, isolating the benefit of conditioning.

**Inspection outcome.** The chamber and atlas laboratory records reviewed are synthetic numerical experiments. Their existing calibration tests do not supply physical launch intervals. The package records physical calibration as NOT_RUN and does not modify or relabel those interfaces.

**Verification.** The exact bridge replay and ten focused tests pass. The three inherited complete nonlinear ODE reconstructions pass again. All source and noise assumptions remain explicit; a failed sufficient test is not a recovery impossibility theorem.

**Next objective, not executed.** Ingest actual launch metrology using the protocol, or add a schema-preserving synthetic calibration adapter to the chamber. A genuine physical capability claim additionally requires evidence that the source model, launch support, clock/velocity conversion, and readout-noise premises describe the apparatus.
