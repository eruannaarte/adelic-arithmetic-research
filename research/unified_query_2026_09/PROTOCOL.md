# Certified query recovery under structured uncertainty

User authorization: develop the next five objectives and the common research line proposed after the fifteen-cycle continuation. Preserve earlier conversations and research artifacts. The deliverable is a local research package; publication and changes to other tasks are outside this step.

## Required outcomes

1. A complete common answer-set and transfer theorem, with explicit source, query, nuisance, noise, approximation, metric, and acquisition contracts. Distinguish exact equivalence from conservative containment and uniqueness from efficient computation. Instantiate the theorem in at least two distinct models and obtain a substantive new bound or obstruction.
2. Harmonic acquisition: extend the isolated interior collision to a quantified neighborhood of schedules, or provide a short schedule certified over the entire product source class. Finite vertex checks alone are insufficient.
3. Preparation: inspect the existing double-pendulum Atlas/lab and validation records; derive a usable joint preparation-calibration protocol with exact acceptance criteria. If real measurements exist, assess their coverage. Simulated validation is not apparatus calibration, and lack of measurements must remain explicit.
4. Quadratic arithmetic: construct an actual-sensor query using uncertain a(7) and the nonlinear a(25) relation, preserving complete tails and the shared noise geometry. Require a supported improvement, a sharper conditional distinction, or a useful proved obstruction.
5. Arithmetic sensing: extend affine nuisance removal to higher polynomial drift, with every new tail channel and conditioning loss charged on the same acquisition and source class.
6. Resolution: certify a smaller asymmetric spatial bank under declared target, time, source, and noise assumptions, comparing actual acquisition counts and error norms explicitly.

Each path records its initial objective before confirmatory work, its baseline, result, proof/certificate, controls, limitations, and evidence-based successor. Failed sufficient gates are preserved and do not become impossibility claims. Candidate searches may use floating optimization; decisions require exact arithmetic, outward enclosures, or complete analytic proofs tied to their models.

## Scope and coordination

The package begins at `research/unified_query_2026_09/`. Dedicated work is isolated in harmonic/, noise/, resolution/, quadratic/, preparation/, and framework/. Root maintains integration and the current-state index. Independent audits cross-check decisive claims after production. All prior packages remain unchanged. Existing expensive verified premises may be inherited explicitly; source identity is not a proof of source validity.

The user reports prior internal testing by Sol and invites inspection of the Double Pendulum AO/Atlas and lab, with a Windows machine through Tailscale available if needed. No physical apparatus or measured calibration dataset has yet been identified. Existing local records are the first source; remote computation is optional and does not substitute for measurements.
