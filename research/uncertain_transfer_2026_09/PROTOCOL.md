# Timing and model uncertainty: declared scope

Started 2026-09-05 before new numerical discovery. Preserve the six completed
research packages byte for byte. Extend certified query recovery to uncertain
clocks and declared model mismatch, with demonstrated consequences in both
active models.

1. Spatial design: retain all 21 targets, all nonzero two-mode sources, every
   time in [1,2], integer row offsets in [-16,16], and minimum separation two.
   The primary seven-row improvement contract is L2 sensor radius 1.1e-7 and
   relative source accuracy below 0.001, charging model and computation errors.
   A separately fixed six-row contract is radius 3e-8 and the same accuracy.
   Either successful complete-time certification advances the cost/noise
   tradeoff. Any obstruction must specify its layout class; unsuccessful
   numerical search alone is not an obstruction. Timing and mismatch consume
   additional explicitly quantified budget and may reduce the sensor allowance.
2. Arithmetic design: retain the actual 8,900 readings, complete infinite-tail
   premises, and verified normal residual. Compare degrees 6, 8, 10 and 12
   under a declared amplitude/frequency family, sensor radius 4e-5, and a
   separate additive weighted mismatch allowance 1e-6. Optimize sufficient
   recovery budgets rather than approximation error alone. State the clock
   family and its effect on unbounded polynomial nuisance before claiming
   timing robustness. Include actual data reconstruction under a nonzero clock
   error and model mismatch.
3. Shared theorem: prove how clock uncertainty and model mismatch enlarge the
   image of an admissible source after nuisance elimination and approximation.
   Preserve correlations or conservatively add deterministic norm bounds.
   Address failure when uncertain acquisition leaks unbounded nuisance; give
   valid positive alternatives and check strict query gates in both models.

Proof, numerical discovery, outward reconstruction and physical calibration
are distinct. Do not infer uncertainty-family membership from solver residuals.
Record meaningful adverse cases, independent review, and reproducible evidence.
