# Completion audit

The two objectives in [PROTOCOL.md](PROTOCOL.md) are complete at the stated
scope. The previous general transfer theorem is unchanged.

| Requested objective | Concrete result | Validation |
|---|---|---|
| Reduce the separated spatial bank with a noise/accuracy tradeoff | Nine rows retain the old allowances; eight rows offer two explicit L2 profiles and retain both H1 profiles | Every case covers [1,2]; all 2,457 rational records pass; full kernel replay; eight focused tests |
| Verify an actual augmented arithmetic solve | Residual recomputed from all readings, phases, weights and polynomial columns; failed floating proposals refined and checked | Four complete recovery cases; two residual identities; 320-bit data and proposal replay; nine focused tests |
| Budget bounded nonpolynomial drift | Joint sensor/remainder/correction/solve-error inequality; a complete Taylor remainder admits a large slow sinusoid | Exact rational gates for all 49 unknown coefficients in each of four cases |
| Keep both aligned with the theorem | The unchanged common gates recover the new interval, pair, inverse and integer guarantees | [applications.json](applications.json): 2,457 interval gates, seven profiles, 196 arithmetic coordinate gates |
| Preserve the research record | Four preceding packages unchanged | 338 historical file hashes checked by [integrity.py](integrity.py) |

The additional seven-row result is a proved ambiguity for one specified bank,
not a global lower bound on seven rows. The arithmetic results are conditional
on the declared coefficient envelope and valid physical sensor/remainder
bounds. The actual solve residual is verified; those physical assumptions are
not inferred from that residual. Spatial floating normalization retains its
previous conditional computed-data allowance.

The evidence uses exact rational checks, outward arithmetic, two evaluation
forms, higher precision and separate finite-grid diagnostics. It is not an
external peer review, apparatus calibration, proof-assistant formalization or
historical novelty assessment. No new physical dataset or public release was
created. Next objectives are recorded in [REPORT.md](REPORT.md); none is needed
to obtain the guarantees already stated here.
