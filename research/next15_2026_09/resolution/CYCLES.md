# Adaptive finite-resolution cycles

Only the current objective is selected in advance. Each successor is chosen after the preceding result and controls are checked. The historical package remains unchanged.

## R1 — Objective recorded before confirmatory work

Certify that moving the exactly central target to any cell in a substantial interior interval preserves at least 99% of the existing finite information floor, at n=1001, K=2, g=4/5 and every tau in [1,2], in L2, continuum H1, and natural discrete H1 calibration. Seek a structural locality theorem for the full noncommuting diffusion–modulation generator rather than a sampled placement sweep. Determine an explicit sufficient boundary distance using exact arithmetic. A failure is useful only if it identifies a proved obstruction or a precise failed bound.

The proposed proof lifts each Gram entry to a diagonal target block of a tensor-product weighted graph semigroup. Uniformization makes low-order terms local walks. Compare translated target blocks before any walk can detect a boundary; bound the remaining Poisson tail. Verify moment agreement and the resulting inequality independently on small matrices, and include a target-at-boundary control. This first objective concerns a fixed realized placement, with a uniform bound over the allowed placements. It does not assume that a decoder knows an unknown target location or establish cross-placement recovery.

Successor: not selected yet.

### R1 result and adaptive decision

Completed: an exact graph-locality theorem yields placement error <3.252891e-12 for every target index41..959, all tau∈[1,2]. All3 calibrated floors retain>99% of their central values. Exact graph walks detect the predicted first boundary-sensitive order; a dense Arb exponential and4 tests pass. This changes the earlier exactly-central guarantee into a uniform919-placement certificate. It does not identify an unknown target or compare different-placement response fibres.

## R2 — Successor objective selected after R1 verification

The remaining distinction is operational: a fixed-placement Gram does not make a decoder available when the target location is unknown. Seek an explicit decoder that first identifies an unknown nonzero target index within a certified interior interval from the same full normalized output, then recovers the two source coefficients. Use the energy centroid of the orthogonally transformed real-space output: an infinite translation-invariant kernel is reflection-symmetric, while the finite boundary error should be controlled by first-moment walk tails. Certify a finite nonzero relative observation-noise tolerance and translate it into the existing source-floor calibration. Zero source must remain an explicit localization obstruction. The usable interval and noise radius will be selected only after the inequalities are evaluated.

Successor after R2: not selected yet.

### R2 result and adaptive decision

Completed: for every unknown target47..953 and nonzero two-port source, the normalized output-energy centroid has certified error<0.279067 at relative output noise1e-4. Rounding identifies the target, then the correctly selected source inverse obeys all3 calibrated floors. Absolute noise3e-8 per unit L2 source (or3e-9 per unit H1 source) suffices for relative source error<1e-4. A full1002001-state floating stencil example recovers target47; four controls and the exact checker pass. Zero source and unrestricted absolute noise at arbitrarily small amplitude remain proved obstructions.

## R3 — Successor objective selected after R2 verification

R2 makes unknown placement recoverable but uses every spatial output coordinate, and its worst noise-centroid bound scales with the full1000-cell lever arm. Seek a different observation operator: restrict readout to one window around a nominal target500, with unknown target offset at most10 cells. Derive a uniform operator-norm loss for discarded output using single-response walk tails, retain at least99% of the R1 floors in all3 metrics, and include the discarded energy's first moment in the localization proof. Determine a concrete window that permits target/source recovery at relative window-output noise1e-3. Count only retained spatial channels, while clearly retaining the global x-average and distinguishing direct spatial acquisition from virtual post-processing of the full modal data. This is a change of sensing operator and nuisance decoder, not merely another target index in R1.

Successor after R3: to be identified from its outcome, not executed as a fourth counted cycle.

### R3 result and stopping decision

Completed: with target prior490..510, a99-row spatial window451..549 loses at most4.923987e-12 information and retains>99% of the original central floors in all3metrics. The window energy centroid localizes every nonzero source at relative window-output noise1e-3, with certified total error<.198181. Source-relative noise3e-7 in L2 or3e-8 in H1 yields relative source error<1e-3. The new operator and first-moment truncation proof are substantive changes beyond R2. A full-grid least-singular-direction noise example,3tests and the exact checker pass. Two symmetric readouts about a central target are proved rank-one and therefore insufficient for two-source recovery.

The next justified objective is to certify fewer asymmetric spatial-average channels under the same21-target prior. It is recorded as a successor only and has not been executed or counted. Exactly3 cycles are complete in this chain.
