# Arithmetic bias and sensor noise: adaptive cycle record

## N1 — objective fixed before confirmatory computation

The previous multiscale construction certifies noiseless bias0.4980274, while target50 iid variance increases relative to the single window. At the same8900 physical readings and existing positive cosine window, investigate the one-parameter inner/outer mixture family. Seek a rigorously supported improvement or Pareto obstruction for the joint objective of deterministic coefficient bias and actual sensor-noise amplification, counting nested observations once. Discovery can use floating-point calculations; a counted outcome needs complete tails and the actual correlated decoder, not a sampled surrogate. Choose N2 only after N1 is proved/certified.

**Confirmatory approach selected after the pilot, before interval verification.** Study the original complete absolute-tail/Neumann bias-certificate functional over every inner weight alpha in[0,1]. A selected finite subset plus the already verified positive remote bounds provides a necessary lower bound for that complete certificate, even though it is not a bound from below on the actual worst bias. Prove that every alpha whose complete certificate could have bias<1/2 lies in a narrow rational interval. Over that entire interval, certify a strict increase in actual coefficient50 iid variance compared with the outer window, using the shared-reading cross term and uniform Gram bounds. Retain the already fully verified reference mixture as a feasible point. This is a Pareto obstruction for the declared certificate/design family, not a claim that certificate failure proves actual recovery impossible.

**N1 result and validation.** Every mixture that could pass the original complete bias certificate must satisfy .0017613<alpha<.0020428. Over that entire closed interval, actual target50 iid variance exceeds the outer-window variance by a factor>1.00009090067766. Complete remote terms are retained, the two finite affine minorants exclude entire half-lines, all new Gram/weight inequalities reproduce at192/256 bits, and4 tests including the direct8900-row covariance pass. The reference alpha still has its verified complete bias. This is a supported obstruction to escaping the original tradeoff by mixture retuning alone.

**Successor justified by N1.** The obstruction concerns the original absolute-bias certificate and unchanged decoder. The source envelope is one-sided and known, whereas that decoder does not subtract its known midpoint. The next question should exploit that information with a different deterministic preprocessing step, while explicitly certifying its full infinite-tail evaluation error.

## N2 — objective fixed before confirmatory computation

On exactly the same8900 observations and d14 coefficient envelope, investigate subtracting the known midpoint of the omitted coefficient interval at every measurement. Its formal value is one half of zeta(2+it)^14 minus the retained d14 Dirichlet polynomial. Prove the complete centered-tail bound and compute the entire deterministic correction vector outward, including a declared rational approximation error. Compare the centered multiscale and centered outer-only decoders under the same actual sensor noise, retaining the original covariance. Seek a materially larger rigorous bounded-noise radius and determine whether the outer-only design now certifies rounding. Do not assume an exactly evaluated infinite sum or omit digital correction error. Select N3 only after this result is proved and checked.

**N2 result and validation.** The complete midpoint correction is now supplied at all8900 readings with strict complex error<10^-20. It halves the complete bias bound while leaving the full physical-noise covariance unchanged. Multiscale rounding certifies eta=10^-4 and outer-only rounding certifies eta=8*10^-5; the reference sufficient radius increases by a factor>127.237. All8 tests pass, including the entire correction at192/256 bits and independent high-precision evaluations. The outer-only acquisition now has a sufficient rounding guarantee, escaping N1's unchanged-decoder obstruction through justified preprocessing.

**Successor justified by N2.** The newly available margin makes a qualitative robustness extension feasible: an instrument may have an unbounded affine baseline error, for which the bounded-error theorem alone provides no guarantee. A constant baseline exactly aliases coefficient1, so its observability boundary must be explicit. With the common normalized-source assumption a(1)=1, the next question is whether a nuisance fit can absorb an arbitrary affine baseline while preserving the certified integer prefix at the same readings.

## N3 — objective fixed before confirmatory computation

At exactly the same8900 readings and centered-tail preprocessing, assume the normalized source a(1)=1 and add an arbitrary complex baseline beta0+beta1*t to every measured value. Investigate an augmented decoder that jointly fits the arithmetic columns and a normalized linear-time nuisance column. Prove a complete tail/noise bound with a certified Schur complement, including the full pointwise infinite-tail envelope for the new nuisance channel. Seek recovery of all50 coefficients, counting a1 as known, at a useful declared sensor-error radius without any magnitude bound on beta0 or beta1. Prove the exact constant-baseline nonidentifiability obstruction when a1 is unknown, and validate the actual augmented observation map rather than a numerical-rank surrogate. Record the subsequent research objective only after this cycle is validated.

**N3 result and validation.** The exact augmented decoder now tolerates arbitrary complex affine baseline at the same readings, recovers the49 unknown retained integers, and supplies the known a1=1. It certifies sensor radii9*10^-5 for the reference and7*10^-5 for the outer design, with digital correction error still included. The complete new nuisance-tail channel uses the full zeta(2)^14 envelope, not a finite surrogate. Schur lower bounds exceed0.99999999999999. The exact unknown-a1 collision proves the hypothesis boundary. All11 chain tests pass, including192/256-bit replay, independent full augmented-matrix controls, a worked large-baseline source that defeats the original decoder, and deliberately invalid contracts/noise budgets.

**Next objective justified by N3, not counted as a completed cycle.** The affine nuisance fit costs less than0.011 additional worst coefficient bias and retains useful margin. Investigate how many polynomial baseline modes can be removed at these fixed readings and a fixed7*10^-5 common sensor radius. Seek a certified maximal degree or a quantitative obstruction, tracking normalization aliases and complete new tail channels for every mode. The affine result makes that question concrete but does not prove the higher-order extension.
