# Actual quadratic arithmetic: adaptive cycle record

## Q1 — objective fixed before confirmatory computation

The preceding package certifies a(5) at weighted sensor radius0.02001, using a(5),a(20),a(45). Determine the exact nearest-label geometry of the entire realizable quadratic prefix through50 in the normalized coefficient metric ||(a(n)/n²)_(n≤50)||₂. This diagnostic can establish whether simply adding more multiplicative relations within that prefix can materially improve the worst-case ideal margin. It is not a claim about the full zeta-series measurement metric. Seek exact pairwise distances for a(5)=0,1,2, with genuinely realizable minimizing prefixes and an explicit proof; use the result to choose the next sensing objective.

**Q1 accomplished.** Full analytic proof establishes all three exact pair distances, with universal half-distance0.02004054555934. Three genuine fundamental discriminants attain them; the auxiliary primes have recursive integer Lucas proofs. The small checker and four controls pass, including all81 sign patterns at2,3,5,7, independent divisor-character coefficient construction and false prime/metric witnesses. The result is sharp in the declared normalized coefficient metric. The existing complete sensor radius is already close to this ideal worst case, so merely adding retained-prefix identities offers little uniform improvement.

## Q2 — successor objective recorded after Q1 validation

Q1 exposes a nearly saturated finite-prefix margin, but its minimizing prefixes can be realized by different fields at every cutoff. Determine the exact infimum of unequal a(5)-label distances when the entire normalized coefficient sequence is observed in l2. Decide whether actual field pairs attain it, and whether nonattainment changes recovery at the critical closed noise radius. Retain genuinely realizable finite approximants and a complete infinite-tail convergence argument. This investigates the infinite-support and compactness issue that finite Q1 cannot resolve; it is still a coefficient-space theorem, not a zeta-sensor noise theorem.

**Q2 accomplished.** The exact distance infimum is sqrt(zeta(8))/25 and no unequal-label field pair attains it. Full-sequence query uniqueness holds for closed noise radii up to and including sqrt(zeta(8))/50≈0.02004073208441, but fails above it. The proof supplies actual CRT/Dirichlet approximants, complete tail bound16/N², and an elementary split-prime strictness argument. The rational series/square-root checker and four independent controls pass. Audit clarified the positive approximation gap in the final midpoint argument. Infinite ideal information improves the finite50 universal radius by only0.000931%, so a substantial uniform gain is not supported by simply adding coefficients in this metric.

## Q3 — successor objective recorded after Q2 validation

Return to the actual8900-reading zeta sensor. Q1/Q2 identify a worst-case all-inert obstruction; they leave open larger useful conditional margins when low primes split. Use correctly recovered a(2),a(3) to exploit every5-multiple≤50 whose multiplier is2,3-smooth, with the same probability measure, full d2 tails, Gram bounds and adversarial weighted noise model. Derive and certify a separate radius for each of the nine a(2),a(3) strata, compare with the original three-coordinate query under identical premises, and retain an implementable finite estimator. Do not claim the coefficient-space ideal threshold applies directly to this sensor or claim a uniform large gain if only conditional gains occur.

**Q3 accomplished.** Full proof and exact certificate supply all nine conditional radii, from0.02003 to0.02324. For a(2)=a(3)=2 the sufficient boundary improves by over14.21% relative to the old three-coordinate query under identical premises. The all-inert theoretical boundary is unchanged, consistent with Q1/Q2's diagnostic. The final rational decoder checks its anchors, selects the certified stratum, and abstains outside its noise budget. All four new controls pass, including the actual8900-row reused-weight operator and worst-direction noise; a full497500-term finite/Gram/remote source replay equals the bound source exactly. The three-cycle suite has12 tests before the independent Q3 audit.

**Next objective drafted, not executed or counted:** investigate a joint finite classifier incorporating uncertain a(7) and the nonlinear a(25) relation, and seek sharp query separation in the actual weighted observation norm with complete tails. This follows from the demonstrated conditional gains and preserves the distinction from ideal coefficient geometry.
