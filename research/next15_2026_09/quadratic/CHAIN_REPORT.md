# Three adaptive cycles on actual quadratic information

This sequence answers two different questions: how much ideal coefficient information can improve the worst-case query margin, and when actual finite measurements can do better for a restricted arithmetic class. The distinction matters. The first two cycles study Euclidean distance between normalized coefficient sequences a(n)/n². The third returns to the actual8,900 zeta readings and their positive weighted sensor norm. No sharp coefficient-space threshold is silently transferred to that observation system.

The starting theorem recovered a(5) for every real quadratic field at certified weighted noise radius0.02001. It exploited a(20)=a(4)a(5) and a(45)=a(9)a(5), after recovering a(2),a(3). Its sufficient boundary was about0.02003156. The first “what next” question was whether more multiplicative relations within the first50 coefficients could produce a materially larger uniform margin.

Cycle Q1 establishes the exact nearest-label geometry of that entire realizable prefix. Write

    J=5⁻⁴+20⁻⁴+45⁻⁴.

The minimum squared distances between labels a(5)=0,1,2 are J, J+4/25⁴, and4J+4/25⁴. To prove the lower bounds, differing coefficients at2 or3 already cost too much to minimize the distance. When those anchors agree, the identities at20 and45 force the displayed contributions; a(25) adds the remaining distinction. The omitted squared coordinates cannot cancel these costs.

The bounds are attained by genuine field prefixes. Prescribe every prime through47 except5 inert, and choose5 inert, ramified, or split. CRT and Dirichlet's theorem realize these patterns. Three explicit fundamental discriminants, together with recursively checked integer primality certificates, verify actual witnesses of the exact minima. This supplies both sides of the geometry theorem, rather than relying on an unrestricted coefficient envelope.

The resulting ideal universal half-distance is0.02004054555934. The previous complete sensor boundary is numerically close, though the metrics differ. More importantly, Q1 proves that adding within-prefix identities cannot change this worst-case coefficient-space limit. That prompted a genuinely different second question: does the complete infinite coefficient sequence remove the obstruction, and is its sharp boundary attained by fields?

Cycle Q2 determines the exact infinite-sequence infimum:

    δ=sqrt(ζ(8))/25.

No unequal-label pair of actual fields attains it. On every index5^(2h+1)m² with5 not dividing m, inert, ramified and split behavior at5 forces zero, odd and even coefficient values. This supplies the universal lower bound. For an actual ramified-at5 field, a split prime elsewhere makes at least one contribution strictly larger. An elementary construction finds such a prime among the divisors of100d−1 for the squarefree radicand d.

The infimum is approached by genuine fields agreeing with the all-inert patterns through successively longer prefixes. A complete tail estimate below16/N² proves convergence; no field realizing infinitely many prescribed inert primes is assumed. The limiting formal sequences belong to the closure of the field responses but are not field responses themselves.

This nonattainment changes the endpoint. Query uniqueness under arbitrary closed-ball noise holds at radius sqrt(ζ(8))/50 itself, approximately0.02004073208441, and fails above it. The finite-prefix endpoint failed because its minimizing pair existed. The infinite theorem asserts uniqueness on promised full-sequence observations, not an efficient algorithm or an infinite physical instrument. Its numerical margin improves Q1 by only0.000931%; the more substantial finding is the role of closure and attainment in a precise arithmetic recovery threshold.

Q2's tiny uniform gain motivated Q3 to seek conditional gains in an implementable finite experiment. Once a(2),a(3) are recovered, every coefficient at5,10,15,20,30,40,45 is a known multiple of a(5). These are exactly the5-multiples through50 whose remaining factors involve only2 and3. A rational linear combination uses all seven, minimizing a declared noise-amplification upper bound while retaining the complete tail bias.

All nine a(2),a(3) strata certify. Their declared weighted-noise radii range from0.02003 when both primes are inert to0.02324 when both split. In the split/split stratum, the sufficient boundary exceeds0.02324532962, a gain over14.21% relative to the original three-coordinate query evaluated under identical premises in that stratum. The all-inert theoretical boundary remains unchanged. The finite decoder checks the anchors, selects the appropriate rational weights, and abstains outside its certified budget.

These results are supported by full proofs, exact rational checkers, genuine field witnesses, complete finite and infinite tail reconstruction, actual shared-grid operator controls, and an independent audit. The old baseline certificate was freshly reproduced without modifying it. Tests reject forged geometry, primality, source, query-weight and budget claims. The results remain conditional on exact observation/model assumptions and an explicit allowance for upstream numerical error.

The next justified question is a joint classifier incorporating uncertain a(7) and the nonlinear a(25) relation, with a sharp bound in the actual weighted observation norm. That question follows from the demonstrated conditional gains; it is recorded but not counted as a fourth cycle.
