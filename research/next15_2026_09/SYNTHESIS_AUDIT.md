# Independent synthesis audit

Auditor: `/root/review_oig`, 2026-09-05. **Verdict: REPORT.md and README.md accurately synthesize the completed mathematical results after the small wording repairs listed below. No material unresolved numerical or scope issue remains.**

This bounded review compared the report and reproduction guide with the completed harmonic, preparation, noise, resolution, and quadratic proofs and audits. It did not repeat their proofs or rerun the package. The recorded consolidated validation reports 18/18 passing jobs, 66 focused tests, and fresh deep preparation reconstruction in 34.005 seconds.

## Corrections made to the report

- The short harmonic schedule has maximum time exactly 29.966619. “Below” that value was changed to “at most.” Its vertex guarantee remains distinct from the exact interior collision and zero full-source factor.
- The leading 127-times improvement now explicitly describes a **certified weighted sensor-noise threshold**, and the preparation table states a volume ratio rather than an ambiguous “exceeds by” comparison.
- The preparation radii are “at or just below” 10^-5, preserving the downward-rounded uniform-design coordinates. The volume statement now says globally certified **near-optimality within the fixed certificate polytope**, rather than suggesting an exactly attained computed optimum or an optimum of the physical inverse problem.
- The finite preparation source domain is explicit: the two logarithmic mechanical parameters and shared clock parameter each lie in [-10^-4,10^-4].
- The resolution setup now explicitly states a two-dimensional source space and **known** time in [1,2]. The localization/inversion theorem does not estimate an unknown time parameter.

No certificate, code, or numerical bound was changed. README.md required no correction.

## Distinctions checked and preserved

**Harmonic:** the necessary horizon bounds use six readings and the declared raw Euclidean metrics. The universal exclusion through 21.8 is separate from one explicit schedule below 30. The exact collision applies to that schedule, not every short schedule; the interval existence proof is not replaced by a floating residual.

**Preparation:** comparisons concern sufficient finite-box diameters and fixed decoders. The primary 10^-5 target failure remains visible. Changing budget shares changes W, while comparisons within each design retain W. The anisotropic volume optimum is restricted to the fixed certificate polytope; no physical calibration necessity or hardware feasibility is inferred.

**Quadratic:** the finite coefficient-space radius excludes its attained equality endpoint. The infinite coefficient-space radius includes its nonattained equality endpoint, for uniqueness on promised infinite observations. Neither ideal threshold is applied directly to the finite zeta sensor. The greater-than-14.21% Q3 gain is conditional on both small primes splitting and compares the same stratum, complete tails, and physical weighted metric. The all-inert theoretical boundary is unchanged. The linked audit now covers all Q1–Q3 results and the enforced decoder-input validation.

**Noise:** N1 limits the specified complete absolute-tail/Neumann certificate family, not every recovery procedure. N2 includes the entire omitted envelope and the complete rational correction error; deterministic centering preserves its original decoder covariance. N3 changes the decoder, requires a1=1, and recovers only the other 49 coefficients from the observations. Its unbounded affine-drift theorem is an exact-solve statement, with amplitude-dependent finite-precision effects explicitly excluded from that guarantee.

**Resolution:** the three source calibrations remain distinct. Full-field placement stability, unknown-target localization, and restricted spatial acquisition have different premises. The 99-channel claim uses a narrower target prior and a changed output operator; it is not a universal tenfold noise improvement. Each channel retains a global source-coordinate average, and post-processing already acquired modal data does not establish a physical acquisition saving.

## Reconstruction and chronology

The report correctly describes five adaptive three-cycle sequences with overlapping work, rather than one globally ordered chain of fifteen experiments. Tests, corrections, and documentation are not counted as extra cycles. The README's 13 checker commands and five suites agree with the recorded 18 jobs and 66 tests. Newly reconstructed harmonic/noise quantities, exact arithmetic consequences, and deep nonlinear preparation enclosures are distinguished from the separately replayed quadratic source and inherited degree-14 tail and central-resolution premises. Hashes identify those premises; neither the report nor guide presents them as proofs. The unchanged expensive baselines are not mislabeled as new research cycles or new full reconstructions.
