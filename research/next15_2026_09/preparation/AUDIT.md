# Independent audit of the three preparation cycles

Auditor: `/root/review_oig`, 2026-09-05. **Verdict: all three stated results are supported under their explicit model and arithmetic premises.** The audit found no numerical certificate gap. One comparison sentence was clarified during review: changing budget shares changes W; the selected/uniform comparison preserves the budget-to-noise rule and eta, while each decoder comparison holds its own W fixed. The author repaired the wording without numerical changes.

## Mathematical review

**Cycle 1.** The fixed-decoder argument in [PROOFS.md](/Volumes/KINGSTON/Vibecoding/Math/research/next15_2026_09/preparation/PROOFS.md:11) correctly integrates first along the source segment with preparation fixed, then along the preparation segment with source fixed. Convexity keeps both segments inside the validated boxes. Applying B before taking the preparation support function preserves exactly the cancellation permitted by a shared launch error. The comparison x <= R x + v and the weighted Cauchy–Schwarz noise bound imply the stated finite feasible-set diameter. The theorem permits any fixed left inverse because its actual weighted row noise gains are recomputed; it does not silently assign a least-squares gain to arbitrary left inverses.

The augmented field has 31 coordinates with the correct six derivative directions and identity initial preparation Jacobian. The endpoint clock derivative is tau times the nominal-time field, including exp(e), so it handles a finite shared clock. The analytic positive mass determinant supplies local smoothness. The polynomial defect and strict first-exit tube inequality justify the outward propagation and continuation on the accepted compact tubes. I inspected the inherited interval jet, polynomial substitution, nonfinite rejection, strict gate, and rational export helpers. The general gate permits equality when both quantities vanish, but this producer gives every tube radius a strictly positive floor; the recorded finite steps meet the intended strict first-exit condition.

The smooth-flow hypotheses match Teschl's parameter-dependence discussion and Theorems 2.10–2.11 in the [author's primary text](https://www.mat.univie.ac.at/~gerald/ftp/book-ode/ode.pdf). The [Arb primary paper](https://fredrikj.net/math/arbpaper.pdf) supports the arithmetic dependency, not the correctness of the application proof.

**Cycle 2.** The interval row bounds are precisely a convex absolute-center term plus the sum of absolute decoder coefficients times interval radii. The weighted noise norm is convex, and its conservative linear bound makes the stated epigraph candidate program valid. The rational correction b = btilde + (e_i - btilde A) B0 restores bA = e_i exactly. All final bounds use the corrected decoder rather than the optimizer's nominal equality. The row-specific maximum-coordinate proof and the capped preparation/noise half-planes are correct. The primary rho = 10^-5 failure remains visible; the successful 7e-6 revision reuses the larger validated box and the already fixed decoder. Failure of the old sufficient gate is not reported as an inverse-problem impossibility.

**Cycle 3.** The eight coordinates are exactly the active A+B launch coordinates. The cap remains the outward-validated 10^-5. The normalization G_ij = rho0 H_ij / b_i correctly includes the factor two in the diameter budget. Multiplying Gx <= 1 and x <= 1 by nonnegative multipliers gives the advertised total budget C; ordinary AM–GM then gives U = (C/8)^8 / product(a_j). Therefore V/U lower-bounds the achieved fraction of the maximum volume over this fixed certificate polytope. The actual box volume factor (2rho0)^8 cancels in comparisons. No optimizer convergence or KKT claim is required.

## Independent verification performed

Using `/private/tmp/math-five-paths-venv/bin/python`, I ran `verify.py --deep`: all three full nonlinear ODE artifacts, including source bindings and all scientific fields, reproduced exactly. I also ran the complete 17-test suite; all tests passed. This included the independent mechanical matrix-solve field and finite preparation/clock derivative diagnostics. These floating diagnostics remain controls, not proof substitutes.

A separate standard-library calculation, importing none of the producer/checker functions, enumerated endpoint corners for **135 decoded interval entries** across all three synthesized designs. Every R and H entry agreed exactly; the same calculation independently checked BA = I, the design-dependent weighted noise norms, both volume duals, the isotropic comparisons, and the eighth-power geometric-mean targets. The resulting normalized volumes were approximately 0.6270072214 and 0.6300536635849732. Their rigorous primal/dual ratios were approximately 0.9999999999801651 and 0.999999999462356, and their isotropic volume gains were 5.265502584924518 and 5.186904106479452.

Additional exact controls checked a nontrivial sharp anisotropic case: G has one constraint 2x1 <= 1, with all other bounds coming from the unit caps. The primal and dual both give volume 1/2. The arbitrary-left-inverse family A=(1,0)^T, B=(1,M) checked why its actual noise gain squared is 1+M^2. Four additional mutations of sensor, time, cost, and launch were rejected. The existing adversarial tests reject wrong sharing, clocks, shares, malformed intervals, invalid decoders, violated half-planes/caps, negative dual multipliers, and modified coordinate/derived-value bindings.

## Scope retained

These are rigorous improvements of sufficient nonlinear certificates for the specified finite source box, preparations, sensors, and budget-dependent deterministic noise metric. They neither establish hardware feasibility nor order the true minimax errors of different designs. The volume near-optimality is only over the fixed decoder's validated calibration polytope. Trust remains in the analytic enclosure argument, Python rational arithmetic, and Arb's outward primitives; this is not proof-assistant verification. The recorded chain contains three substantive changes—shared nuisance projection, fixed decoder synthesis, and globally bounded anisotropic volume—with the failed intermediate target and an unexecuted successor preserved.
