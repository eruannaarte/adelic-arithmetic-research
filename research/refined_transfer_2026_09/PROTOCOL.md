# Refined transfer: scope fixed before the new searches

Started 2026-09-05 in response to the request to pursue the selected next
objectives. Preserve all five completed research packages byte for byte.

1. **Seven-row spatial design.** Fix the minimum L2 sensor radius at 1e-7,
   relative to source norm, before searching. Retain every target 490,...,510,
   every nonzero two-mode source, every known time in [1,2], and source relative
   accuracy below 0.001. Allow seven distinct integer row offsets in [-16,16]
   with separation at least two. Charge the existing 1e-9 model error and
   1e-9 numerical allowance. Either produce a complete-time certificate at this
   contract or prove an obstruction for an explicitly stated nontrivial class
   of layouts. A search failure is not an impossibility theorem. Any weaker
   contract explored later must be labelled separately.
2. **Verified normalization.** Accept actual unnormalized exact rational
   readings and known rational time; compute a rational normalized vector with
   a certified physical error charge. Discharge the existing conditional
   numerical budget without knowing the source amplitude or its target. Reject
   unresolved or inconsistent inputs and retain the physical sensor/model
   assumptions. Do not turn exact time into an uncharged uncertain-time claim.
3. **Weighted drift approximation.** Specify a smooth drift family and degree
   eight. Replace the pointwise Taylor remainder by a verified bound in the
   actual physical weighted norm, retaining all 8,900 readings, the complete
   arithmetic tail, and the actual normal-residual verifier. Demonstrate a
   certified improvement and show where the family assumptions enter.

Each objective needs a complete argument, executable verification, meaningful
adverse checks, and clear limitations. Connect the new guarantees to the
unchanged general transfer theorem. Numerical discovery is distinct from proof;
model calibration and empirical apparatus validation remain separate questions.
