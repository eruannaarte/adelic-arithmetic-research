# Combined acquisition and publication objectives

Started 2026-09-05 before the new searches. Preserve all eight predecessor
research packages. Complete the selected mathematical objectives, then present
the transfer theorem and its underlying paths in a shareable interactive lab.

1. Six-row structured uncertainty: retain rows (485,493,499,501,507,515),
   all 21 targets/all nonzero two-mode sources, nominal and true times in
   [1,2], sensor radius 3e-8 and source relative accuracy below 0.001. Fix
   shared clock radius 1e-8 and spatial potential radius |g-4/5|<=1e-8 before
   testing. Charge complete approximation, normalization and nonlinear
   remainders. Prove the combined contract or a scoped obstruction; do not
   infer it by joining the two existing separate certificates.
2. Arithmetic: improve the complete clock bound with larger multiplicative
   blocks or phase geometry after the exact nuisance projection, retaining
   the entire divisor envelope and all omitted-class/remainder charges.
   Compare at the existing clock rectangles and full sensor/drift budgets.
3. Degree eleven: construct and verify the missing degree's complete-tail,
   weighted approximation, Gram and actual inverse certificate. Test B=4000,
   Omega=3, sensor radius 4e-5, mismatch 1e-6, base affine-clock radii 1e-14
   and 1e-11, and required coefficient error below 1/2. Establish whether this
   sufficient certificate passes; failing it is not a universal impossibility
   theorem. Record computation cost and higher-precision replay.
4. Presentation: create an accessible, responsive, standalone interactive
   Transfer Theorem Lab connecting the original five research paths to the
   common answer-set framework and the completed two-model consequences.
   Distinguish proved certificates, numerical illustrations and model premises.
   Provide reproducible presets, shareable state, downloads and evidence links.
5. Publication: publish necessary reproducibility code/evidence and the lab to
   the user's specified GitHub repository, with an immutable release and a
   self-contained publication package. Send that finished package to the
   existing Website Manager task in Autonomous Radio Explorer. The website
   task manages production integration; do not treat delivery as live website
   publication.

Every mathematical result requires a proof, independent checks and a route
back to the model. The presentation must survive offline use and direct file
opening and must not label arbitrary browser calculations as new rigorous
certificates. Preserve source classes, physical units, shared nuisance and
uncertainty-family limitations. No historical novelty or apparatus calibration
is claimed without separate evidence.
