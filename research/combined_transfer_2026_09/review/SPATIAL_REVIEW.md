# Independent review of the combined six-row transfer

The degree-eleven worker reviewed the spatial proof and its producer/consumer
boundary independently. No mathematical gap was found in the stated fixed
contract. This review does not extend the clock, potential, source class or
time domain. It leaves all 26 sealed spatial files unchanged.

The review re-executed the centered consumer at **384 bits**, including all
2,688 target/time direction cells and all 1,280 exact-polynomial source/pair
records. The complete all-time covers, positive Gram minors and all stored
cell bounds passed. The reviewer separately recomputed the combined rational
remainder and two source gates without calling `check.consequence` for those
calculations. Changed clock and potential rectangles were rejected. Exact
results are in [spatial_review.json](spatial_review.json); the reproducible
review is [spatial_review.py](spatial_review.py).

The complete model requires all 21 target labels, every nonzero vector in the
two prescribed orthonormal source modes, nominal and true times in [1,2], six
specific rows, and source-relative Euclidean sensor radius 3e−8. The physical
potential family and preparation/readout remain declared model premises. The
new proof is for this full source/time domain, not only its synthetic examples.

The derivative approximation is taken from differentiated physical kernels,
with complete Cauchy, periodic-image and finite-boundary remainders; it is not
obtained by differentiating a uniform value-error inequality. For this six-row
bank every displacement is at most 25 and therefore lies inside the existing
maximum-26 kernel contract. The current consumer uses twelve matrix entries
when turning scalar remainders into operator errors. The prior nominal and
derivative kernel replay commands remain the route to those physical premises;
this review did not rerun their full coefficient producers.

The joint linear direction uses the same pair (dt,dg) at all six readings.
Rectangle-corner convexity is valid for both the forward and inverse matrix
norm. Forming the polynomial Gram and inverse numerators before whole-cell
interval evaluation retains the weak-direction cancellation. The nonlinear
charge includes dt²/2, 128|dt·dg|, (128/3)dg², and both derivative approximation
charges. Positivity and the generator-norm comparison hold throughout the
small declared potential family. The mixed term is charged once; no independent
clock/model draws or commutation of the generator pieces is assumed.

The crucial acquisition combination is established by **new** pair inequalities
at tube radius 3.25e−8. The previous 3.2e−8 radius would not contain the combined
uncertainty charge. Strict positive definiteness, followed by weighted
Cauchy–Schwarz with the rational split, separates different target tubes for
arbitrary source amplitudes and directions. The two competing explanations
may choose different admissible clocks and potentials; the argument never
forces a common unknown parameter across hypotheses.

The independently recomputed total radius is
4864502567503/150000000000000000000, strictly below 3.25e−8. With
mu=361/(256·10^9), the source gate is

`(eta+rho+xi+r)^2 < (1/1000−V)^2 mu`, with `1/1000−V>0`.

It passes and yields the advertised upper error below 0.000852831068. The
coarser gate `(eta+rho+xi+r+F)^2 < mu/1000^2` also passes. Accordingly this
rectangle demonstrates the combined six-row/nonzero-uncertainty contract;
it does not show that directional inversion is necessary at this size.

The raw decoder's two different solves are appropriate: completion of the
relative-tube quadratic uses `P^T P−b^2 I`, while the reported nominal least-
squares source uses `P^T P`. Its normalization uses the known nominal time.
The source-to-output norm upper bound remains 4/3+eta because the true time
stays in [1,2] and the true generator stays positive semidefinite, so that
wrapper does not silently assume a known true clock. Data digitization still
belongs in the sensor premise. The decoder checks a supplied admissible
interval; it cannot certify that the physical apparatus actually lies there.
