# Objective: complete oscillatory nuisance channels

The preceding polynomial-drift package certifies degrees one through five for the multiscale weights, and one through four for the outer weights. Its degree-six complete bias bounds exceed 0.6116 and 0.6617, respectively. These are failed sufficient bounds, not recovery impossibility statements. Its nuisance-channel estimate is the full pointwise centered Dirichlet tail multiplied by the actual weighted absolute polynomial norm.

This extension keeps the 8,900 physical readings, both declared positive weight vectors, integer envelope 0<=a(n)<=d14(n), known a(1)=1, and exact polynomial nuisance space. The primary objective is a positive all-49-coordinate degree-six recovery certificate obtained by sharper complete nuisance-tail channels. The new calculation must retain every omitted source coefficient, including the infinite remote part, and freshly connect all new bounds to the actual sampling operator.

The initial strategy is to split each new channel at a finite coefficient cutoff. Bound its oscillatory physical response throughout the finite frequency range; control the entire remote mass analytically or by an outward zeta-minus-prefix computation. A finite cutoff or small sampled response alone is not accepted as a channel certificate. Computational preprocessing and inverse-error allowances must pass through the same physical weighted metric and source scaling.

After degree six is proved, a bounded extension of degree may be used only to quantify the resulting nuisance-complexity/noise tradeoff. Report failed sufficient gates accurately. Acceptance requires complete proofs, outward model reconstruction, a strict exact consumer, meaningful adverse controls, and a clear transfer interface for the root framework. All previous artifacts remain unchanged.

## Initial exploration, before confirmatory computation

A preliminary moment bound gives a centered remote mass below about 188.5 at cutoff 10^6, using zeta(1.69)^14/(2*10^(6*0.31)). This is smaller than the previous complete pointwise envelope, about 469.812, but it does not yet bound the finite oscillatory channel. No recovery claim follows from this exploratory estimate.

## Confirmed core and bounded successor

The first accepted certificate uses cutoff 10^12 and two discrete summation-by-parts steps. Its complete degree-six maximum biases are below 0.249284107184 for the multiscale design and 0.298235974205 for the outer window. Both the full outward producer and the independent exact consumer pass, at declared sensor radii 10^-4 and 8*10^-5. Thus the degree-six objective is achieved before extending the search.

The evidence-based successor is a bounded comparison at degrees 8, 10 and 12, keeping the same cutoff, norm, source class, complete channel proof and observations. This tests the new method's nuisance-complexity/noise tradeoff; it does not seek a globally maximal polynomial degree. The original failed pointwise degree-six gate remains an explicit control.
