# Structured uncertainty: scope fixed before discovery

Started 2026-09-05. Preserve all seven completed research packages. Pursue all
objectives selected from the uncertain-acquisition report.

1. Spatial potential: keep the certified seven rows and all sources/targets,
   with nominal and true time in [1,2]. Specify H(g)=I tensor Lx + Ly tensor
   diag(1+g*x), nominal g=4/5. Bound structured potential and timing directions,
   including complete remainders, and their effect on the weakest source
   direction. Compare against the prior generic generator norm allowance at
   the same sensor radius 1.1e-7 and source relative accuracy 0.001. Obtain a
   certified tolerance improvement or a quantitatively proved limitation.
2. Six-row acquisition: pursue the previously fixed contract, sensor radius
   3e-8, model and numerical radii 1e-9 each, source relative accuracy below
   0.001, all 21 targets/all nonzero two-mode sources/all known times in [1,2],
   row offsets [-16,16] with minimum separation two. Require complete-time
   certification or a proved obstruction for an explicitly delimited class;
   search failure is not an impossibility theorem. Do not silently lower this
   contract to obtain a positive result.
3. Arithmetic clock: retain the full integer divisor envelope, 8,900 physical
   weighted readings and complete tails. Replace the global pointwise clock
   derivative charge by a complete weighted bound using the actual phases,
   and, when justified, their projected influence on the requested integers.
   Retain the shared affine clock so unbounded polynomial drift remains in
   its nuisance space. Demonstrate a strict certified improvement, preserving
   correlations between slope/offset, source coefficients and readings.
4. Degree and cost: fix B=4000, Omega=3, sensor radius 4e-5, mismatch radius
   1e-6, and coefficient accuracy below 1/2. First compare degrees 6/8/10/12
   under the same clock radii 1e-14 and 1e-11 and future solve residual ceiling
   1e-30. Select the least tested degree satisfying every complete bound.
   Charge matrix dimension and arithmetic work, and report measured runtime
   with its environment. Larger clock tolerances may be separately declared
   and certified after the comparison. Do not call a failed sufficient gate
   an information-theoretic lower bound.
5. Shared theorem: retain the joint incidence of uncertainty directions after
   the correct nuisance operation. Prove structured support/inverse bounds
   with complete nonlinear remainders, instantiate them in both models, and
   identify any improvement versus independent norm addition.

Deliver concise self-contained proofs, actual-model reconstructions, executable
consequences, meaningful adverse checks, independent review, and a reproduction
guide. Synthetic validation and calibration remain distinct. Historical novelty
or optimality requires separate evidence.
