# Canonical numerical environment for artifact replay and GitHub Actions.
#
# requirements.txt and pyproject.toml retain the supported dependency ranges
# for ordinary use. Apply this file as a pip constraint when replaying the
# committed numerical reports. Platform-scale floating differences are checked
# separately by the strict-schema numerical replay comparator.
numpy==2.4.6
scipy==1.15.2
mpmath==1.3.0
gmpy2==2.3.1
python-flint==0.9.0
